# dreamTerm — Agent Dashboard

Live agent dashboard for the VM fleet at .

## What's inside

| File | Purpose |
|------|---------|
|  | Dashboard API server (port 4000) — Firebase Auth + tmux session list |
|  | Dashboard UI — terminal iframes, canvas iframe, screenshot button |
|  | Helper script that starts ttyd for a tmux session |
|  | Audit script — lists all tmux sessions, detects agent types |
|  | Kills all tmux sessions with no active agent |
|  | Operations skill doc |

## Quick start


